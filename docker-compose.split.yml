services:
  composure:
    build:
      target: api-only
    environment:
      SERVE_FRONTEND: "false"
      CORS_ORIGIN: ${CORS_ORIGIN:?Set CORS_ORIGIN to your deployed frontend origin (for example https://your-app.pages.dev)}
